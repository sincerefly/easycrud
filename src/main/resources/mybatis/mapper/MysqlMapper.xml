<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.yasking.easycrud.data.mapper.MysqlMapper">

    <!-- 查询数据库版本 -->
    <select id="getVersion" resultType="String" >
        select version()
    </select>

</mapper>